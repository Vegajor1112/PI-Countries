const defineProcess = require("./defineProcess");
const checkDatabase = require("./checkDatabase");
const fetchData = require("./fetchData");
const buildStatement = require("./buildStatement");

module.exports = { defineProcess, checkDatabase, fetchData, buildStatement };
